#!/bin/sh
set -e

per=$(pamixer --sink 0 --get-volume)
per=$((per"$1"5))
pamixer --sink 0 --set-volume $per
if xdotool search --onlyvisible i3bar && pgrep &>/dev/null -a -f i3mon.py; then
    printf "♬ $per%%\x002">/tmp/i3.fifo
else
    echo "$per%" | dzen2 -p 2 -fn 20
fi
