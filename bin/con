#!/bin/sh
set -eu
test -n "${1-}" || {
    find $HOME/x/con -printf "%P "
    echo
    exit
}

! test "$1" = "add" || {
    test -z "${2##/*}" && p=$2 || p=$PWD/$2
    exec ln -s $p $HOME/x/con/${3-}
}

file=$HOME/x/con/$1
shift

for arg; do printf "%s\000" "$arg"; done | xargs -0 mpv $file
