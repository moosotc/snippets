#!/bin/sh
set -e

url=${1-$(xclip -o -se c)}
hsh=$(basename "$url" | md5sum | cut -d' ' -f1)
out=$HOME/.cache/llpp/$hsh.pdf

prince -s $HOME/xsrc/snippets/misc/prince.css -o "$out" "$url"
llpp -origin "$url" "$out"
