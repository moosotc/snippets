#!/bin/sh
set -e

mt=$(file -b -L --mime-type "$1")
case "$mt" in
    video/* | audio/*) o=mpv;;
    application/pdf) o=llpp;;
    application/msword) o=llppac;;
    *) echo "eh? $mt"; exit 111;;
esac
exec $o "$@"
