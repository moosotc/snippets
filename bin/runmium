#!/bin/sh
set -e

false && {
    exec 1>/tmp/chro.out
    exec 2>/tmp/chro.err
} || {
    exec 1>&2 2>/dev/null
}

test -z "$@" && {
    xdotool windowactivate $(xdotool search --class 'chromium' | tail -1) &&
        exit 0 || true
}

exec chromium "$@"
