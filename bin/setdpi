#!/bin/sh
set -e

if true; then
    output=$(xrandr | awk 'FNR==2{print $1}')
    xrandr --dpi $output
    echo "Xft.dpi: 190" | xrdb -merge
else
    xrandr --dpi 161
    echo "Xft.dpi: 161" | xrdb -merge
fi
