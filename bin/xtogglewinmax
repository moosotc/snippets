#!/bin/sh
set -e

# FIXME: 94 54 26 4
vonly="$1"
id=$(xdotool getactivewindow)
geom=$(xprop -notype -id $id GEOMOO)
expr >/dev/null "$geom" : ".*: .*" && {
    geom=$(xdotool getwindowgeometry --shell $id)
    eval $geom
    xprop -id $id -format GEOMOO 8s -set GEOMOO "X=$X Y=$Y W=$WIDTH H=$HEIGHT"
    test -z "$vonly" && {
        xdotool getactivewindow windowsize 100% 94% windowmove 0 54
    } || {
        eval $(xdotool search --class i3bar getwindowgeometry --shell) X=$X
        xdotool getactivewindow windowsize 0 94% windowmove $X $((HEIGHT+26))
    }
} || {
    set -- $geom
    eval eval ${*:3}
    xprop -id $id -remove GEOMOO
    xdotool windowsize $id $W $H windowmove $id $((X-4)) $Y
}
